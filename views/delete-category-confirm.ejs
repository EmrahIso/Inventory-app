<!doctype html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <link rel="stylesheet" href="/style.css" />
    <title><%= title %></title>
  </head>
  <body>
    <div class="container">
      <h1>Delete Category: <%= category.name %></h1>
      <% if (typeof error !== 'undefined' && error) { %>
        <div class="form-errors">
          <ul>
            <li><%= error %></li>
          </ul>
        </div>
        <% } %>
      <div class="form">
        <p style="margin-bottom: 15px">
          Are you sure you want to delete this category?
        </p>
        <p style="margin-bottom: 20px">
          <strong>Warning:</strong> This will permanently delete the category
          "<%= category.name %>" and all items inside it. This action cannot be
          undone.
        </p>
        <form
          method="POST"
          action="/delete/category/<%= category.id %>"
          style="display: inline; margin-right: 10px"
        >
          <div style="margin-bottom: 15px">
            <label for="admin-password" style="color: red; font-weight: bold"
              >Admin Password (Required):</label
            >
            <input
              type="password"
              id="admin-password"
              name="admin-password"
              required
              style="
                border: 2px solid red;
                display: block;
                margin-top: 5px;
                padding: 8px;
              "
            />
          </div>
          <button type="submit" class="btn delete-btn">
            Yes, Delete Category
          </button>
        </form>
        <a href="/category/<%= category.id %>" class="details-btn">Cancel</a>
      </div>
    </div>
  </body>
</html>
